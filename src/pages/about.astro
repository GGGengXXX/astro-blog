---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`关于 | ${SITE_TITLE}`} description="About this personal note garden." />
		<style>
			.about-wrap {
				display: grid;
				grid-template-columns: minmax(0, 0.95fr) minmax(280px, 0.55fr);
				gap: 1rem;
			}

			.about-main,
			.about-side {
				padding: clamp(1.25rem, 3vw, 2rem);
			}

			.about-main p {
				max-width: 720px;
				color: #3f3b34;
				font-size: 1.08rem;
			}

			.about-list {
				display: grid;
				gap: 0.75rem;
				margin: 2rem 0 0;
				padding: 0;
				list-style: none;
			}

			.about-list li {
				padding: 1rem;
				border: 1px solid var(--line);
				border-radius: 6px;
				background: rgba(250, 248, 241, 0.58);
			}

			.about-side {
				background: var(--ink);
				color: var(--paper);
			}

			.about-side p {
				color: rgba(250, 248, 241, 0.68);
			}

			.stack {
				display: grid;
				gap: 0.7rem;
				margin-top: 1rem;
			}

			.stack span {
				padding: 0.75rem 0.85rem;
				border: 1px solid rgba(250, 248, 241, 0.18);
				border-radius: 6px;
			}

			@media (max-width: 820px) {
				.about-wrap {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="about-wrap">
				<div class="about-main surface">
					<div class="eyebrow">About</div>
					<h1>关于这个地方</h1>
					<p>
						这是 GGGengXXX 的个人记录空间。它可以同时放技术笔记、日记、读书摘录、项目复盘和还没想清楚的念头。
					</p>
					<p>
						我把它设计成“个人花园”而不是正式出版物：内容可以先粗糙地存在，之后再慢慢修剪。能持续留下来，比一开始就漂亮完整更重要。
					</p>
					<ul class="about-list">
						<li><strong>笔记</strong><br />保存命令、配置、读到的概念和操作清单。</li>
						<li><strong>日记</strong><br />记录当天发生了什么，以及当时的判断和状态。</li>
						<li><strong>想法</strong><br />还没有成熟，但值得以后回看的问题、假设和灵感。</li>
					</ul>
				</div>

				<aside class="about-side surface">
					<div class="eyebrow">Stack</div>
					<h2>站点结构</h2>
					<p>Astro 负责生成静态页面，GitHub Actions 负责自动部署，Markdown/MDX 负责写作。</p>
					<div class="stack">
						<span>Astro</span>
						<span>Markdown + MDX</span>
						<span>GitHub Pages</span>
						<span>Content Collections</span>
					</div>
				</aside>
			</section>
		</main>
		<Footer />
	</body>
</html>
